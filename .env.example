# Environment Configuration for Bluetooth Manager
# Copy this file to .env and adjust the values

# Authentication credentials (leave empty to disable authentication)
BT_USERNAME=admin
BT_PASSWORD=bluetooth123

# Flask secret key (change in production)
SECRET_KEY=bluetooth-switch-secret-key-change-me

# Host IP Address (auto-detected if not set)
# HOST_IP=192.168.1.100

# Client Name (uses hostname if not set)  
# CLIENT_NAME=MacBook-Pro

# Flask Environment
FLASK_ENV=production

# Ports (default values)
HOST_PORT=5002
CLIENT_PORT=5001

# Debug mode (set to true for development)
DEBUG=false

# Example usage:
# cp .env.example .env
# Edit .env with your credentials
# docker-compose --env-file .env up
